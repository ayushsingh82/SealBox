# Agent PFPs

- **agent-1.svg** – Saved from your raw SVG (gray/red robot).
- **agent-2.svg**, **agent-3.svg**, **agent-4.svg** – Currently copies of agent-1 so the generate page works.

To use your other 3 designs: replace the contents of `agent-2.svg`, `agent-3.svg`, and `agent-4.svg` with the full SVG code you pasted (the teal/green one, the pink/red one, and the purple one). The generate page will then show all 4 distinct agents.
